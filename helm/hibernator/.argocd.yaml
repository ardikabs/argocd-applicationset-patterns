namespace: hibernator-system

deployments:
  backend:
    devel:
      enabled: true

    stg:
      enabled: true

      syncPolicy:
        syncOptions:
          - CreateNamespace=true

    prd:
      enabled: false

clusterExclusions:
  stg-backend-eks-cluster-2: true
